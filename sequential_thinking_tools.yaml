name: Sequential Thinking
serverUserType: singleUser
shortDescription: MSB benchmark server (sequential_thinking_tools)
description: |
  MSB benchmark server `sequential_thinking_tools` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8103/mcp
