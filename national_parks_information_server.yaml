name: National Parks
serverUserType: singleUser
shortDescription: MSB benchmark server (national_parks_information_server)
description: |
  MSB benchmark server `national_parks_information_server` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8111/mcp
