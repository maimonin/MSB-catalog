name: Bright Data
serverUserType: singleUser
shortDescription: MSB benchmark server (bright_data)
description: |
  MSB benchmark server `bright_data` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8108/mcp
