name: Linear
serverUserType: singleUser
shortDescription: MSB benchmark server (linear)
description: |
  MSB benchmark server `linear` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8110/mcp
