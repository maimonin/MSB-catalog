name: Desktop Commander
serverUserType: singleUser
shortDescription: MSB benchmark server (desktop_commander)
description: |
  MSB benchmark server `desktop_commander` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8106/mcp
