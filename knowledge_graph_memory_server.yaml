name: Knowledge Graph Memory
serverUserType: singleUser
shortDescription: MSB benchmark server (knowledge_graph_memory_server)
description: |
  MSB benchmark server `knowledge_graph_memory_server` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8104/mcp
