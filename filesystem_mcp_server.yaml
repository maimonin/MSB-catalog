name: Filesystem
serverUserType: singleUser
shortDescription: MSB benchmark server (filesystem_mcp_server)
description: |
  MSB benchmark server `filesystem_mcp_server` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8107/mcp
