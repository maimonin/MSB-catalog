name: Notion
serverUserType: singleUser
shortDescription: MSB benchmark server (Notion)
description: |
  MSB benchmark server `Notion` (npx via stdio->HTTP runner). For MCP Security Bench experiments.
metadata:
  categories: MSB
runtime: containerized
containerizedConfig:
  image: msb-npx-runner:local
  port: 8099
  path: /mcp
  args:
    - "-y"
    - "@smithery/cli@latest"
    - "run"
    - "@smithery/notion"
startupTimeoutSeconds: 300
