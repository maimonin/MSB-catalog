name: Met Museum
serverUserType: singleUser
shortDescription: MSB benchmark server (met_museum_server)
description: |
  MSB benchmark server `met_museum_server` (runner container, registered as remote). For MCP Security Bench.
metadata:
  categories: MSB
runtime: remote
remoteConfig:
  fixedURL: http://172.17.0.1:8102/mcp
